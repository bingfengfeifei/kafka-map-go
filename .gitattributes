# Exclude frontend code from language statistics
# The frontend is embedded into the Go binary, so Go is the primary language
web/** linguist-documentation

# Exclude build artifacts
cmd/server/web/** linguist-generated

# Mark the primary language
*.go linguist-language=Go
